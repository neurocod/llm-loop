__pycache__/
*.py[cod]
*.egg-info/
.venv/
venv/

# Build backend output. `pip wheel .` and `python -m build` leave both behind,
# and `pip install -e .` leaves the egg-info above inside src/.
build/
dist/
.pytest_cache/

# The C++ gate is built INTO hooks/, next to hooks.json, because that is where
# its `../bin/<tool>` paths resolve from -- not because it is source. It is a
# Windows-x64 image built from ask_user_gate.cpp; build it with cpp/build.py.
claude-plugin/ask-user-gate/hooks/ask_user_gate.exe
claude-plugin/ask-user-gate/hooks/ask_user_gate
claude-plugin/ask-user-gate/hooks/*.pdb
claude-plugin/ask-user-gate/hooks/*.ilk
