name: tests

on:
  push:
    branches: [main]
  pull_request:
  workflow_dispatch:

jobs:
  pytest:
    # Both operating systems earn their place: the engine drives a terminal
    # status line, spawns provider CLIs and locks files, and every one of those
    # differs on Windows. A Linux-only matrix would pass while the tool the
    # author actually runs is broken.
    runs-on: ${{ matrix.os }}
    strategy:
      fail-fast: false
      matrix:
        os: [ubuntu-latest, windows-latest]
        # The floor declared in pyproject (requires-python = ">=3.9"). Listing
        # it here is what turns that declaration into a tested claim rather
        # than a hopeful one.
        python-version: ["3.9", "3.10", "3.11", "3.12", "3.13"]

    steps:
      - uses: actions/checkout@v4

      - uses: actions/setup-python@v5
        with:
          python-version: ${{ matrix.python-version }}

      # Installing the project rather than pointing pytest at src/ means the
      # packaging metadata is exercised on every push: a pyproject that no
      # longer builds fails here instead of at release time.
      - name: Install
        run: python -m pip install -e ".[test]"

      - name: Test
        run: python -m pytest -q

      # The ask-user-gate plugin carries its own pins and is outside
      # `testpaths`, so pytest never reaches it. Both are stdlib-only and
      # platform-guarded, which is why they can ride the same matrix: it is the
      # only thing keeping the gate's path resolver and its wiring check honest
      # on a machine that is not the author's.
      - name: Test the ask-user-gate plugin
        run: |
          python claude-plugin/ask-user-gate/hooks/ask_user_gate.py --self-test
          python claude-plugin/ask-user-gate/bin/try_patch.py --selftest
